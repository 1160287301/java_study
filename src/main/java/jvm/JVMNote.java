package jvm;

/**
 * 方法区
 *  方法区存储了每一个类的结构信息,方法区是规范,在不同虚拟机里面的实现是不一样的,最典型的就是永久代(PermGen space)和元空间(Metspace)
 *  类似于 List list = new ArrayList();
 *        方法区 f = new 永久代;
 *        方法区 f = new 元空间;
 *
 * stack
 *  栈管运行,堆管存储
 */
public class JVMNote {


}
